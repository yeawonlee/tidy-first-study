## 1장. 보호 구문 (early return)

- 중첩된 조건 → 보호 구문을 적용하면, **전제 조건이 명시적으로 드러날 때 분석하기 쉽다**

```tsx
// 중첩 조건
if (조건)
    if (!다른 조건)
        ...코드...
        
// 보호 구문 적용
if (!조건) return
if (다른 조건) return
...코드...
```

- **남용하지는 말자**. ex) 7~8개의 보호 구문이 있는 루틴은 읽기 어려움